name: status-guard

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]

jobs:
  status-guard:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "STATUS enforcement removed; status-guard is a no-op."
