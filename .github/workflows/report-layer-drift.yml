name: Report Layer Drift Gate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  report-layer-drift:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run drift check
        run: python3 tools/check_report_layer_drift.py
