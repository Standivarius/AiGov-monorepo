# Scenario Library

Place authored scenario instances here (e.g., `scenario_001_title.yaml`).

Do not store templates or schemas in this folder.

## Catalog rules
- Schemas define structure only (see `packages/specs/scenarios/schemas/base_scenario_v1.schema.json`).
- Catalog policy (naming, IDs, uniqueness, coverage) is enforced by validators, not schemas.
