# Scenario Templates (Canonical)

This folder contains the single canonical template(s) used to create new scenarios.

Rules:
- Use versioned, kebab-case names: `scenario-template-vX.Y.yaml`.
- Do not add new variants in other locations; extend the canonical template instead.

Current canonical template:
- `scenario-template-v1.2.yaml`
