# Module Cards

This directory contains module registry and module cards for GDPR-only workflows.

## Export module cards to XLSX

Run:

```
python3 tools/export_module_cards_to_xlsx.py
```

Output:
- `outputs/module_cards.xlsx`

The `outputs/` directory is intentionally untracked.
