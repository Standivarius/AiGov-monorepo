# file_export_fail_symlink

This fixture is a normal directory seed.

Symlink failure is tested portably by the validator, which copies this fixture to a temp directory and creates a symlink at runtime.
